package es.um.pds.tableros.domain.card;

import java.util.Optional;

public interface CardRepository {
    // TODO: Definir métodos que la infraestructura deberá implementar.
    // Ejemplo: void save(Card card);
    // Ejemplo: Optional<Card> findById(CardId id);
}