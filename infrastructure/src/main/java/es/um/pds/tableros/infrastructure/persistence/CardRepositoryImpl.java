package es.um.pds.tableros.infrastructure.persistence;

import es.um.pds.tableros.domain.card.CardRepository;
import org.springframework.stereotype.Repository;

@Repository
public class CardRepositoryImpl implements CardRepository {

    // TODO: Inyectar la interfaz de Spring Data JPA (ej. SpringDataCardRepository).
    
    // TODO: Implementar los métodos de CardRepository (save, findById).
    // TODO: Traducir el objeto 'Card' del dominio a 'CardEntity' antes de guardar en BD.
}